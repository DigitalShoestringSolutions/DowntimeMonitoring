{
    "sources": [
        {
            "topic": "{{mqtt_topic}}",
            "filter":{},
            "metric": "{{metric}}",
            "value": {{value}},
            "comparison": "{{comparison}}",
            "target": "{{target_uuid}}"
        }
    ],
    "mqtt": {
        "in": {
            "broker": "{{broker_in}}",
            "port": 1883
        },
        "out": {
            "broker": "mqtt.docker.local",
            "port": 1883
        }
    }
}